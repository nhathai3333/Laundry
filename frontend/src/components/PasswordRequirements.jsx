/**
 * Component hiển thị yêu cầu mật khẩu
 * Đã bị vô hiệu hóa - không hiển thị gì
 */
export default function PasswordRequirements({ password = '', showOnly = false }) {
  // Component đã bị vô hiệu hóa - không hiển thị yêu cầu mật khẩu
  return null;
}
